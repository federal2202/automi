/**
 * Google Calendar hooks
 *
 * Public entry point preserved for backwards-compatible imports. The
 * implementation now lives in `./google`, split into per-concern modules
 * (query keys, queries, mutations, cache utilities) each under 100 lines.
 */

export * from './google'
