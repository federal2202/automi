/**
 * Default time window applied to newly added schedule entries and the create
 * form's seed entry (Monday 09:00–10:00).
 */
export const DEFAULT_START = '09:00'
export const DEFAULT_END = '10:00'
