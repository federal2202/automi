/**
 * BigCalendar format configurations — public barrel.
 *
 * Grouped by concern: prop getters, static config objects, and the composed
 * config builder.
 */
export * from './prop-getters'
export * from './config'
export * from './builder'
